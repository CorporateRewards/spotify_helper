$(".track-listing.vote-btn").on("ajax:success",function(e, data, status, xhr){
$('#tracks-table').html("<%= j (render 'tracks/track_vote_listing.html') %>")
});

$(".home-page.vote-btn").on("ajax:success",function(e, data, status, xhr){
  $('#home-playing').html("<%= j (render 'tracks/currently_playing.html') %>")
});
