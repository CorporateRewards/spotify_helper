<% if @user %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this file from being uploaded:</h2>
   
      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% end %>
<% if @user %>
      <ul>
      <% @user.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
<% end %>


<h2>Enter the user details below</h2>

<br>
<%= render 'form' %>

<%= link_to 'Back to user list', admin_users_path %>
