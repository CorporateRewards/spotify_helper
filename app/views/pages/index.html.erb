
		<table class="table">
		<th>Track name</th><th>Artist</th>
		<% @playlist.tracks.each do |track| %>

		<tr>
		<td><%= track.name %></td>
		<td><%= track.artists.map(&:name).join(', ') %></td>
		</tr>

		<% end %>
		</table>

        




